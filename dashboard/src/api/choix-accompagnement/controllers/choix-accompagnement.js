'use strict';

/**
 * choix-accompagnement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::choix-accompagnement.choix-accompagnement');
